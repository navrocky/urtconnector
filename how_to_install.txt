To build and install this application run this commands in terminal (of course, in this folder):

In linux/unix you have to:

$ ./build_gcc_linux_release
$ cd release
$ make install


Project at google code:
http://code.google.com/p/urtconnector/