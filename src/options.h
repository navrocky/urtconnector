#ifndef OPTIONS_H
#define OPTIONS_H

/*! \brief Options base class with possibility of saving in XML format.
*/
class Options{
public:
    Options();
    virtual ~Options();
//     virtual void load() = 0;
//     virtual void save() = 0;
};

#endif
